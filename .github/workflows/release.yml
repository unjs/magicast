name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@v2.0.0
    with:
      publish: true
    permissions:
      contents: write
      id-token: write
